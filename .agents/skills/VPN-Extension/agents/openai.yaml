interface:
  display_name: "VPN Extension"
  short_description: "Repo-specific patterns and workflows for VPN-Extension"
  default_prompt: "Use the VPN-Extension repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true